#include <stdio.h>
#include <stddlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;
	char low;

	for (d = '0'; d <= '9'; d++)
		putchar(i + '0');
	for (low = 'a'; low <= 'f'; low++)
		putchar(c);
		putchar('\n');

	return (0);
}
